parseInt("11000", 2); //переводит строку с двоичной записью числа в число.
n.toString(2); //получает для числа n запись в 2-ной системе в виде строки.
alert("messege"); //выводит сообщение в окошке
console.log("messege") // выводит сообщение в консоль
str.indexOf("подстрока") //возвращает позицию подстроки в str, или -1 если не нашёл.